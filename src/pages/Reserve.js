import React from "react";

const Reserve = () => {
    return <div style={styles.reserveStyles}>Reserve</div>;
};

const styles = {
    reserveStyles: {
        marginTop: 40,
        textAlign: "center",
        fontSize: 50
    }
};

export default Reserve;
